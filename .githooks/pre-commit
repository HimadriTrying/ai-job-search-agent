#!/usr/bin/env bash
# Versioned pre-commit hook. Enable once with:  git config core.hooksPath .githooks
# Delegates to the private-data guard.
exec "$(git rev-parse --show-toplevel)/scripts/check-private.sh"
